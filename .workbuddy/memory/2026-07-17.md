# 2026-07-17

## 深度分析 awesome-llm-apps 并产出 bsc-backend 结合升级方案

- 解压 `C:\Users\34216\Downloads\awesome-llm-apps-main.zip`（2253 文件，13 大类）到临时目录 `C:\Users\34216\AppData\Local\Temp\tmp.5IVumZoInt\awesome-llm-apps-main\`。
- 重点研读相关模板：self-improving-agent-skills、evals、knowledge_graph_rag_citations、corrective_rag、trust_gated_agent_team、ai_agent_governance、generative_ui_agents。
- 实测 bsc-backend 真实现状（关键纠偏）：
  - 知识库（app/knowledge RAG：Query Rewrite/Agent Router/权限/Self-RAG/评估/反馈）是**独立生产级系统，但未接进编译器 agent**。
  - ADR-003 的 9 段 DAG **仅落地 5 段**（planner/business_architect/sop_builder/reviewer/presenter）；kpi/risk/conversation 段及其 Agent 未实现。
  - 当前 risk 是关键词启发式 `app/agents/risk_agent.py`（RISK_CATEGORIES + SEVERITY_KEYWORDS），**不是用户想要的 Constraint System**。
- 产出 5 个结合升级方案（A 方法论库可验证引用 / B Risk=Constraint System 重做 / C 自进化+Evals / D 生成式 UI 仪表盘 / E 可信审计整合），写入 `docs/awesome-llm-apps-integration-analysis.md`。
- 推荐顺序 B→A→D→C，已预警"全铺开会冲垮现有回归纪律"。
- 待用户定优先级（plain-text A/B/C 选项，因该用户客户端不渲染 AskUserQuestion UI）。

## 用户选定：先攻方案 B（Risk=Constraint System 重做），用 superpowers 方法论落地
- 调用 superpowers-main 元技能 → writing-plans 子技能，产出 TDD 实现计划：
  - 路径 `docs/superpowers/plans/2026-07-17-risk-constraint-system.md`。
  - 10 个任务：constraint 数据模型 / AuditChain 哈希链 / ConstraintEngine 覆盖+门禁 / RiskArchitectAgent / schemas 注册 RiskModel / state 加 risk 段 / 引擎 DAG 化(sop∥risk 真并行 asyncio.to_thread) / rerun 下游闭包+打回 risk / API 注册 / 全量回归。
  - 复用既有 BaseAgent、ProjectDraftRepository、FakeLLM 测试桩；每个任务含完整代码+pytest 命令（writing-plans "No Placeholders" 要求）。
  - 关键设计：约束权威来源=requirements；覆盖引擎双向（requirement 被覆盖 + BM 元素被治理）；gate=pass|warn|block；审计链 SHA-256 防篡改。
  - 范围边界（YAGNI）：KPI 段未实现，RiskArchitectAgent 已预留 kpi 入参。
- 下一步：向用户给执行方式选项（Subagent-Driven 推荐 / Inline Execution），待其选择后落地。

## 执行落地：Subagent-Driven（10 任务全完成，feat/risk-constraint-system 分支）
- 用户选 A（Subagent-Driven）。全程未碰 main/master，独立分支 `feat/risk-constraint-system`（从 master 带脏树切出，保护用户其它未提交改动）。
- 11 个提交，20 文件，+639/−42。每任务独立 subagent 提交 + 两阶段复核（spec 合规 + 代码质量）。
- **关键纠偏/偏差（已文档化，待最终报告告知用户）**：
  1. Task3 引擎语义：原 plan 的 `evaluate()` 按 element 计算覆盖并门禁未覆盖 element id，与自身 3 个测试矛盾 → 改为"需求满足型覆盖"（约束被 BM 元素引用 id/name 或 SOP 列 covers_constraints 即满足；高危未满足→block）。
  2. Task4 测试夹具：flow id 由 f1 改 r1（让需求 r1 治理它，覆盖达 100%）。
  3. pytest 模块名冲突：tests/constraint/test_engine.py 与 tests/orchestrator/test_engine.py 同名 → `git mv` 改名 test_constraint_engine.py。
  4. Task8 risk 回环：`RiskArchitectAgent.run` 不接受 `fix_instructions`，去掉该参数（偏差）。
  5. Task7 reviewer 重跑上游：把 risk 注入 reviewer 的 _upstream_for（确保闭包正确）。
  6. e2e 回归：`test_e2e.py` 硬编码 agents 缺 risk → 加 K stub + "risk":K() + assert risk in state。
- **环境铁律**：全局 `python` 缺 numpy 且 conftest 要求 → 所有 pytest 必须走 `venv/Scripts/python.exe`（项目 venv 解释器）。
- 最终回归：`venv/Scripts/python.exe -m pytest tests/orchestrator tests/constraint tests/agent -q` → **38 passed**（评审修复前）。collect-only 全树 432 collected，仅 1 个无关错误（test_export_boundary.py 缺 `docx` 依赖，预存/env 问题，非本次改动引起）。

## 评审（finishing-a-development-branch，用户选 B）
- 逐文件通读已提交代码，整体一致、门禁正确、可单测。
- **发现并修复 1 处真实缺陷（847fdcd）**：`app/constraint/engine.py` 的 `evaluate()` 只从 `risk_payload` 读 `overall_score`，从不设 `risks` → `ConstraintResult.risks` 恒为 `[]`，`RiskArchitectAgent` 把 LLM 风险清单传入却被丢弃。已修：`risks = rp.get("risks", [])` 透传。新增 2 测试锁定（test_risk_architect 端到端存活 + test_risk_list_propagates_from_payload）。
- 复测 → **39 passed**（原 38 + 新增 1）。
- 设计级观察（非 bug，不阻塞合并）：① CoverageReport 的 total/covered/coverage_pct 度量约束满足度，elements[].covered 度量单元素治理，两维度共用一对象易误读；② CoverageModel 无 elements 字段，经 RiskModel 校验丢 per-element 覆盖；③ run_pipeline fork 直接 self.agents["risk"]，缺 risk 会 KeyError（生产路径已注册，安全）。
- 仍**未合并 master**（等用户点头发合并）。

## 评审观察打磨（用户选「先打磨 3 个观察点再合并」，d74586f）
- 打磨 3 个评审观察点（非 bug）：
  1. CoverageReport/CoverageModel 双维度（约束满足度 vs 单元素治理）命名易误读 → 两处加 docstring 澄清。
  2. schemas.CoverageModel 无 `elements` 字段，经 RiskModel 校验丢 per-element 覆盖 → 补 `elements: List[ElementCoverageModel]`，新增 test_risk_segment_preserves_element_coverage。
  3. run_pipeline fork 直接 `self.agents["risk"]` 缺 agent 会 KeyError → 加 guard（skip risk 段留空），新增 test_pipeline_runs_without_risk_agent。
- 复测 `tests/orchestrator tests/constraint tests/agent -q` → **41 passed**（原 39 + 新增 2）。
- 分支现 13 提交，**仍未合并 master**（等用户点头发合并）。

## 合并完成（用户指令「合并」）
- 诊断：当前 feat/risk-constraint-system；`master..feat`=13 提交，`feat..master`=0；merge-base==master（YES）；脏树 258 文件但与本次 20 文件**零重叠**（安全切 master）；无活跃 pre-commit hook。
- 执行：`git checkout master` → `git merge --no-ff feat/risk-constraint-system -m "merge: Risk=Constraint System rework"`，ort 策略，20 文件 +730/−42。
- merge commit `6ca8a92` 落定 master 顶部。合并后：`master..feat`=0（feature 全进 master），合并前 258 脏文件原封未动，master 仅多 1 个 merge commit。
- **合并后回归**：`venv/Scripts/python.exe -m pytest tests/constraint tests/orchestrator -q` → **39 passed**（约束+编排全绿，merge 健康）。
- 未做（无指令）：① 删本地 feat/risk-constraint-system 分支；② 推远端（无 remote push 执行）；③ 推进下一个升级选项（A/C/D/E）。

## 清理收尾（用户选 A）
- `git remote -v` 为空 → **无远端，跳过 push**。
- `git branch -d feat/risk-constraint-system` 成功（was d74586f）。因 `master..feat`=0（全合并），安全删除通过。master 顶部仍 `6ca8a92`。
- 现本地分支仅剩：master / feature/knowledge-hardening-round2 / feature/mcp-real-llm（均非本次改动）。

## 下一步候选（待用户定，plain-text A/B/C）
- A. 推进下一个升级方案（原 5 选项剩 A 方法论库可验证引用 / C 自进化+Evals / D 生成式 UI 仪表盘 / E 可信审计整合）。推荐顺序 A→D→C。
- B. 文档收尾：把 Risk=Constraint 的 risk fork + loopback 补进 README / ADR-003（当前 ADR 仅 5 段 DAG，未记这次新增的并行分支）。
- C. 收工：本升级项（B）彻底关闭，转其它项目/需求。

## 启动方案 A（方法论库可验证引用）— 调研 + 计划
- 用户选 A（推进下一个升级，推荐序 A→D→C）。本次只做调研 + 写 TDD 计划，未动手写实现。
- **关键现状核对（实测，防 B 式假设错）**：
  - `grep -rn "app.knowledge" app/orchestrator/` = **0 匹配** → 编译器 agent 完全没接方法论库（确认独立）。
  - `KnowledgeService.retrieve(project_id, query)` 返回 chunk dict 含 `chunk_id/content/section/idx/score/doc_title/domain`；**强隔离：project_id 必填否则返 `[]`**。
  - `app/knowledge/answer.py` 已有 `build_context()`（产出 `citations[{index,chunk_id,doc_title,section,offset,score,snippet}]`）+ `validate_citations()` —— 引用机制现成，仅在 `/knowledge/ask` 内部用。
  - 编译器 agent 覆写 `BaseAgent.run()`，自行拼 user_prompt 调 `llm_service.chat(...)` → 注入点明确在 `run()`。
- **计划**：`docs/superpowers/plans/2026-07-17-methodology-citations.md`，6 任务 TDD：
  1. MethodologyBridge（包 retrieve + 复用 answer citation 形态）；2. SopBuilder 接入 + source_ref；3. BusinessArchitect 接入；4. 引用校验 + citation_coverage 指标；5. engine 透传 project_id（含无文档优雅降级 golden 测试）；6. 全量回归。
  - 范围 YAGNI：只接 sop_builder + business_architect 两个 agent；不动 UI（D）、不动 risk（后续复用同一桥）、不并审计链（E）。
  - 分支 `feat/methodology-citations`（待建），不碰 master。

## 方案 A 实现（Subagent-Driven，全完成，未合并 master）
- 分支 `feat/methodology-citations`（从 master `6ca8a92` 切出，保护 258 脏文件）。8 提交：a40614d(plan) fa98471(T1 桥) 93b745d(T2 sop) 23d4892(T3 ba) 25f31cc(T4 校验+覆盖率) 9393a64(T5 engine 透传) 50b9757(fix 覆盖率存活) 9f02b88(docs)。
- 全量回归 `tests/constraint tests/orchestrator tests/agent` → **53 passed**（A 前 41）。
- **复核抓出的真实缺陷（50b9757）**：Task4 subagent 把 `_citation_coverage` 放 `run()` 顶层，引擎 `state["sop"]=out.get("sop")` 只取子段 → 指标在生产被丢（同 B 风险清单被丢模式）。修：内联进 `sop`/`business_model` 子段 + 同步修正 sop 单测断言位置。
- 关键事实：`ProjectDraft` 无 project_id → 引擎用 `session_id` 当知识库 project_id（已知简化）；`KnowledgeService.retrieve` 形参顺序与计划不同 → 桥统一关键字调用；business_architect 用 `project` dict 作 query 源。
- 复用 `answer.build_context` citation 形态，为方案 E（审计整合）留零成本接口。
- 待用户点头发合并 master（同 B 纪律：不碰 master 无授权）。

## 方案 A 评审（finishing-a-development-branch，用户选 B）
- 逐文件通读 8 个提交（app/orchestrator/methodology.py, sop_builder.py, business_architect.py, engine.py + 4 测试）。
- 唯一真实缺陷（实现阶段复核已修 `50b9757`）：`_citation_coverage` 原置 `run()` 顶层被引擎丢弃 → 已内联进子段。评审时复核确认修复到位。
- `engine.py` diff 确认：project_id 透传 architect(直调+_upstream_for)、sop(fork 两分支+_upstream_for)、rerun_node 经 _upstream_for，均覆盖。
- `methodology.py`：`validate_source_refs` 边界正确（空 items→0.0；空 citations→全标记；source_ref 非 list 安全降级；除零防护）；`derive_methodology_query` 非空兜底。
- e2e 用忠实复刻真实 agent 方法论路径的 FakeSop/FakeArchitectAgent + MethodologyBridge(FakeService) 跑通引擎→桥→source_ref 全链 + 无文档降级。
- 设计级观察（非 bug，不阻塞合并）：① e2e 用 fake agent 而非真实 agent 类（真实 agent 全 run 含 SOPReportEngine 由单测覆盖，组合即全链）；② sop/architect 各自独立检索（未去重，合理）；③ project_id 暂用 session_id（已知简化）；④ source_ref/_citation_coverage 已入 state，待方案 D 渲染。
- 复核后未再改动代码；全量回归 53 passed（评审前已跑）。等用户点头发合并 master。

## 方案 A 合并（用户授权"合并"）
- `git checkout master` + `git merge --no-ff feat/methodology-citations` → merge commit `c550e34`。9 文件变更、820 插入/18 删除。
- 诊断安全：`master..feat=8`、`feat..master=0`、merge-base==master（master 全落后）、脏树 256 文件与本分支 8 文件**零重叠**、无活跃 hook。
- 合并后回归 `tests/constraint tests/orchestrator tests/agent` → **53 passed**；`master..feat=0`（全进 master）；256 脏文件原封未动。
- 未删本地分支 `feat/methodology-citations`（用户只说"合并"，未要求清理；同 B 纪律，删分支需另授权）。
- 用户随后选 A 清理：`git branch -d feat/methodology-citations`（master..feat=0 确认全合，安全删除，was `9f02b88`）。方案 A 整条链路（调研→计划→6 任务→评审→合并→清理）彻底关闭。

## 方案 D 推进（用户选 A 推进下一个升级，推荐顺序 D→C）
- 调研先验证再计划（同 B 踩坑教训）：`app/api/dashboard.py` 是运维监控非产物仪表盘；分析文档称"已有 KpiPanel/RiskPanel"是**假**（src/ 仅 SopPanel）。`Workspace.tsx` 只渲染 BusinessGraph+SopPanel；store 无 `risk` 字段；SopPanel 不渲染 `source_ref`。
- **路由是显式列表注册**（`app/main.py:216`），新增模块不自动挂载→必改脏的 main.py。排查出 3 个干净且已注册 api 模块：`stream_api`/`knowledge_ws`/`orchestrate`；选 **`app/api/orchestrate.py`（CLEAN）** 挂新路由 `GET /api/orchestrate/dashboard/{session_id}`，零碰脏文件。
- Task 1-4 subagent 落地：后端重排接口(3 测试) + 前端 client+store 字段 + 3 面板(RiskPanel/ConstraintCoveragePanel/CitationPanel, premium glass 风格) + MethodologyDashboard 容器 + App.tsx tab 切换(工作台/产物仪表盘)。
- **脏文件避坑**：必不碰 `src/api/orchestrateApi.ts`、`src/components/Workspace.tsx`（脏）；可改 `src/App.tsx`、`src/store/workspaceStore.ts`（干净）。
- Task 3 首个 subagent **静默失败（空返回）**，重派后成功（commit `7934efc`）——后续长任务 subagent 返回异常要立即核验文件/提交是否存在，勿凭空返回判定完成。
- 全量回归 `tests/constraint tests/orchestrator tests/agent tests/api/test_compiler_dashboard.py` → **56 passed**（A/B 53 + D 3）；`npm run check`(tsc) 0 错误；256 脏文件全程未动。
- 分支 `feat/generative-ui-dashboard` 6 提交（66daebb..aaa1033），未合并 master。待用户点头发合并。

## 方案 D 合并（用户授权"合并"，即上一条的 a）
- 诊断安全：`master..feat`=6、`feat..master`=0、merge-base==master（master 全落后）、9 个 feature 文件与脏树 256 文件**零重叠**、无活跃 hook。
- 执行：`git checkout master` → `git merge --no-ff feat/generative-ui-dashboard -m "merge: add interactive compiler output dashboard (方案 D)"` → merge commit `a22b304`，ort 策略，10 文件 +593/−3。
- 合并后核验：`master..feat`=0（feature 全进 master）、`feat..master`=1（仅 merge commit 在 master 不在 feat，符合预期）、256 脏文件原封未动；`git branch --show-current`=master。
- 合并后回归未重跑（D 的 3 测试已在合并前 feature 分支 `56 passed` 验证；无代码改动进入 merge，仅历史图变化，风险低）。
- 待用户定收尾（plain-text A/B/C）：A. 删本地 `feat/generative-ui-dashboard` 分支（master..feat=0 安全）；B. 推远端（`git remote -v` 为空 → 实际无远端可推）；C. 推进下一升级（剩 C 自进化+Evals / E 可信审计整合）。D 整条链路（调研→计划→4 任务→评审→合并）完成，仅缺分支清理。

## 方案 E 推进（用户睡前授权：能自主推进的全力推进，需确认项压到最后）
- 用户原话："请执行以下内容，我现在要睡觉了，所以需要我确认的项目请放到最后，把你那边能推进的部分一并推进。由于工作量大，请适时创建工作管理文件，边记录进度边推进。"
- 自主决策：选 **方案 E（可信审计整合）**（缝合 A 引用 + B 覆盖），全程在特性分支、不碰 master、不删分支、不推远端；需要确认项统一压到最后清单。
- 任务清单（TaskCreate #19..#26）：调研 / 计划 / 内核 / 接入端点 / 测试 / 前端面板 / 全量回归+文档 / 待确认项。工作管理文件 = `docs/superpowers/plans/2026-07-17-trusted-audit-integration.md`（计划+执行日志合一，live 更新）。
- 调研（#19）：实测确认 B `AuditChain.append/verify`（SHA-256、prev_hash 串接、genesis=0×64）；A 引用落点 `sop.sops[i].source_ref` + `business_model.{flows,roles,rules}[i].source_ref`；dashboard 端点负载 `state` 可直接喂审计构建器。
- 实现（#23/#22）：`app/audit/trusted_chain.py`（build/verify/collect）；`app/api/orchestrate.py` dashboard 端点追加 `trusted_audit` 段（复用 CLEAN 已注册路由，零碰脏 main.py）。
- 测试（#25）：`tests/audit/test_trusted_chain.py`(5) + `tests/api/test_dashboard_trusted_audit.py`(1)，含篡改 entry output_hash / 篡改 convenience 字段两类防篡改用例。
- 前端（#21）：`src/components/TrustedAuditPanel.tsx` + `compilerDashboardApi.ts` 加 `TrustedAudit` 类型 + 接入 `MethodologyDashboard` 栅格（premium glass，仅改 D 干净文件）。
- **踩坑修复（真实缺陷）**：初版 `verify_trusted_audit` 误用 `entries[0].get("output")` 做交叉校验 → 但 `AuditEntry` 只持久化 `output_hash` 不持久化原始 `output` → happy path verify 返回 False。改为由 `record["source_refs"]` 反推 `{"source_refs":...,"count":...}` 的 `_sha256(_stable(...))` 与链内 citation 节点 `output_hash` 比对（密码学绑定）。修复后 9/9 测试通过。
- **关键纪律（用户睡前 unattended，规避 subagent 静默失败）**：本次未用 subagent，改为**直接实现 + 持续核验**（每步跑测试/tsc/status 验证），更可靠。
- 全量回归（#26）：`tests/constraint tests/orchestrator tests/agent tests/audit tests/api/test_compiler_dashboard.py tests/api/test_dashboard_trusted_audit.py` → **62 passed**（A/B 53 + D 3 + E 6）；`npm run check`(tsc) 0 错误；合并前 256 脏文件零改动。
- 提交 `e36d029`（特性分支 `feat/trusted-audit-integration`，master `a22b304` 起）：9 文件 / +511。核验 `MASTER..FEAT=1`、`FEAT..MASTER=0`、STAGED=0（用户文件零进暂存）、用户脏文件完整保留（229 未暂存修改 + 27 未跟踪 = 256）。
- **未提交即险**：实现完成后一度 `MASTER..FEAT=0`（忘了 commit，9 文件仅在工作区），及时发现后只 `git add` 我的 9 个显式路径再 commit，避免误 stage 用户 256 脏文件。
- 待用户醒来确认（plain-text A/B/C/D，全部不自主执行）：A. 合并 `feat/trusted-audit-integration` 进 master（--no-ff）；B. 删已合并分支 `feat/generative-ui-dashboard`；C. 加 remote 并 push（当前无 remote）；D. 下一步方案 C（自进化+Evals）。

## 方案 E 合并 + 老分支清理（用户醒来授权 A+B）
- 用户醒来指令："先 A 合并 E，B 顺手清掉老分支，然后 D 收个尾。C 偏重，可以最后做" → 解读：A 合并 E（授权）、B 删老分支、方案 C（自进化+Evals，重）压到最后做；未提 push（无 remote，跳过）。
- **诊断安全**：current = `feat/trusted-audit-integration`；`MASTER..FEAT(E)=1`、`FEAT..MASTER(E)=0`、merge-base==master（YES）；feature 9 文件与脏树**零重叠**（其中计划文档一度因"提交后又补执行日志未提交"而显脏，已 `git commit --amend` 进 `14a7020` 后归零）；无活跃 hook。
- 执行：`git checkout master`（dirty 257 原封）→ `git merge --no-ff feat/trusted-audit-integration -m "merge: 缝合 A 引用 + B 覆盖为可验证 SHA-256 可信审计链 (方案 E)"` → merge commit **`6009e54`**。
- 合并后核验：`MASTER..FEAT(E)=0`（E 全进 master）、`FEAT..MASTER(E)=1`（仅 merge commit）、257 脏文件原封未动（用户 256 + 1 无关杂项，均非我的文件）。
- **全量回归复核（master 重跑）**：`tests/constraint tests/orchestrator tests/agent tests/audit tests/api/test_compiler_dashboard.py tests/api/test_dashboard_trusted_audit.py` → **62 passed**；`npm run check`(tsc) **0 错误** → 确认 merge 健康无回归。
- **B 删老分支**：`git branch --merged master` 确认 `feat/generative-ui-dashboard` 已含于 master（merged at `a22b304`）→ `git branch -d feat/generative-ui-dashboard`（was `aaa1033`）成功。
- 剩余本地分支：`master` / `feat/trusted-audit-integration`（已合，保留，待日后清理）/ `feature/knowledge-hardening-round2` / `feature/mcp-real-llm`（用户原有，未动）。
- **下一步**：方案 C（自进化+Evals）压最后做（重，待用户确认再启动）。可选收尾：删已合 E 分支 `feat/trusted-audit-integration`、推远端（无 remote）。
- 至此 A/B/D/E **四个升级全部合并 master 落地**，仅剩方案 C 待做。

## E 分支清理（用户授权"按照你的建议来" = 采纳建议 A 删已合并 E 分支）
- `git branch --merged master` 确认 `feat/trusted-audit-integration` 已含于 master（`MASTER..FEAT=0`）→ `git branch -d feat/trusted-audit-integration`（was `14a7020`）成功。
- 本地分支现仅剩：`master` / `feature/knowledge-hardening-round2` / `feature/mcp-real-llm`（后两者用户原有，未动）。257 脏文件原封未动。
- 方案 C（自进化+Evals）仍按住，待用户点头再启动（建议先做调研+TDD 计划，不急着写重实现）。

## 固化技能：bsc-safe-merge（项目级）
- 把"脏树环绕下安全合并特性分支"的纪律（merge-base 校验 + dirty-tree 零重叠 `comm -12` 检查 + `--no-ff` + 仅显式路径 stage）做成项目级技能：`.workbuddy/skills/bsc-safe-merge/SKILL.md`。
- 涵盖本仓专属坑：venv pytest（全局 python 缺 numpy）、router 显式列表注册在脏 main.py（新路由挂 CLEAN 的 orchestrate.py）、autocrlf 误显脏用 amend 折回、subagent 静默空返回须核验。
- 来源：B/A/D/E 四次合并 + 分支清理反复踩坑迭代出的可复用流程。下次方案 C 合并直接套用。

## 方案 C 启动（用户 12:44 授权"开始"）
- 用户授权按上一条约定"先做调研+TDD计划"启动方案 C（自进化+Evals）。
- 建分支 `feat/self-evolution-evals`（master `6009e54` 起，保护 257 脏文件）。任务 #27 调研 / #28 计划。
- **调研关键发现（实测，防假设错）**：
  - 仓库已有 Evals 雏形但只评 **PRD 输入文本**：`app/core/prd_quality_scorer.py`(PRDQualityScorer, 规则+LLM 两层, 产出 QualityReport/QualityDimension) + `app/core/prd_refiner.py`(PRDRefiner 多轮自优化闭环)。两者均不评编译器*产物*。
  - `app/orchestrator/agents/reviewer.py`(新 DAG) 是 LLM 评审(约束覆盖+漏洞+回环)，产出二元通过/gap 列表，**非带维度评分报告**。
  - `app/core/metrics.py` 是**性能/基础设施指标**(Prometheus)，与质量评分无关 → 排除。
  - **核心缺口**：编译器产物零评测；编译器无自进化闭环；A 引用度/B 覆盖率/E 完整性未聚合成评分报告。
- **范围决策（YAGNI）**：拆两阶段。Phase 1 Evals（编译器产物评分+仪表盘+测试，风险低价值高，本次实施）；Phase 2 自进化（高分回流 knowledge 库写入 + 低分精炼闭环，偏重，需单独确认）。
- **复用边界**：仪表盘端点 `app/api/orchestrate.py` 是 CLEAN 已注册路由（D/E 已改）→ 加 `evaluation` 段安全，零碰脏 main.py；Evals 用"端点即时计算"模式避免动 ProjectDraft schema；`QualityReport`/`QualityDimension` 直接 import 复用；测试走 venv，规则维度无 LLM 依赖。
- 计划文档 `docs/superpowers/plans/2026-07-17-self-evolution-evals.md`（Phase1 T1-T5 详细 TDD + Phase2 T6-T8 outline）。**待用户确认 Phase1 范围后实施**，未动手写实现。

## 方案 C Phase 1 实现（用户 13:00 "继续" 两次催促）
- 直接实现（不走 subagent）：T1 评分器内核 → T4 单元测试（6/6）→ T2 仪表盘端点接入 → T3 前端面板 → T4 仪表盘测试 → T5 全量回归。
- **T1 内核**：`app/evaluation/compiler_evaluator.py`（CompilerOutputEvaluator，5 规则维度：方法论采用度(A _citation_coverage.coverage 0..1 均值×100)、约束覆盖率(B coverage_pct)、风险门禁健康(B gate.decision→pass/warn/block 映射)、审计完整性(E trusted_audit.verified)、结构完整度(sop/bm/risk 段非空占比)）。复用 QualityReport/QualityDimension。缺字段优雅降级 0 分不崩。YAGNI：LLM 深评维度不在 Phase 1 实现。
- **T2 端点**：`app/api/orchestrate.py` dashboard 路由加 `evaluation` 段（先算 trusted_audit → 复用 evaluate + trusted_audit 避免双 compute）。`.dict()` 改 `.model_dump()`（Pydantic v2 deprecation）→ 消 warning。
- **T3 前端**：`src/components/CompilerEvalPanel.tsx`（总分 pill scoreColor/scoreTextColor 着色 + 5 维度条形 + pass/fail 徽章 + 改进建议列表，premium glass 复刻 D/E 风格）+ API 类型 Evaluation/QualityDimension + `MethodologyDashboard.tsx` 栅格 md:col-span-2 接入。
- **T4 测试**：`tests/evaluation/test_compiler_evaluator.py`(6) + `tests/api/test_dashboard_evaluation.py`(2，正常 89 分/空 draft 15 分)。
- **T5 回归**：全量 `tests/constraint|orchestrator|agent|audit|evaluation|api(test_compiler_dashboard+test_dashboard_trusted_audit+test_dashboard_evaluation)` → **70 passed**（原 62 + C 8）。`npm run check`(tsc) **0 错误**。257 脏文件未动。`MASTER..FEAT=1`。
- **小坑修复**：① 仪表盘测试预期分数算错（写 93 实 89）→ 实际 75×0.25+80×0.20+100×0.20+100×0.15+100×0.20=89 → 修断言。② `.dict()` deprecation → `.model_dump()`。
- 提交 `feat(evaluation)`, 特性分支 `feat/self-evolution-evals`, 7 文件。**待用户点头合并 master**（--no-ff，走 bsc-safe-merge 安全协议）。
- Phase 2（自进化闭环：高分产物回流知识库 + 低分精炼）仍按住，单独确认后实施。
- 无远端。用户 257 脏文件全程未动。详见每日日志。

## 方案 C 合并 + 分支清理（用户 13:12 授权 A）
- 诊断安全：`feat/self-evolution-evals`；`MASTER..FEAT=1`、`FEAT..MASTER=0`、merge-base==master；9 个 feature 文件与 257 脏树**零重叠**；无活跃 hook。
- 执行：`git checkout master`（dirty 257 原封）→ `git merge --no-ff feat/self-evolution-evals -m "merge: 编译器产物评测器 + 仪表盘面板 (方案 C Phase 1)"` → merge commit **`4fbf964`**。
- 合并后：`MASTER..FEAT(C)=0`（全进 master）、`FEAT(C)..MASTER=1`（仅 merge commit）、257 脏文件原封未动。
- 合并后回归 **70 passed**（master 重跑，确认 merge 健康无回归）。
- 分支清理：`git branch --merged master` 确认全合 → `git branch -d feat/self-evolution-evals`（was `522a835`）成功。本地仅剩 `master` + 用户原有的两个 feature 分支。
- **至此 A/B/D/E + C/Phase1 五个升级全部合并 master 落地**。Phase 2（自进化闭环）仍按住，待单独确认。
- 无远端、无 push。

## 方案 C Phase 2 实施（用户 13:18 授权 A）
- 用户授权按"a"（继续）启动 Phase 2。建分支 `feat/plan-c-phase2-self-evolution`（master `4fbf964` 起，保护 257 脏文件）。任务 #34-#38。
- **关键调研发现（防误判）**：`app/knowledge/feedback.py` **早已有完整 FeedbackStore 闭环**（FeedbackRecord/FeedbackStore/FeedbackAnalyzer/add_feedback/get_recent/get_stats/analyze_problematic_queries）。原本 outline 担心的"知识库写入路径缺失"是误判——Phase 2 不需要新建任何存储设施，写一个薄 bridge 把 Phase1 评测结果映射进现有基础设施即可。
- T1 桥 `app/evolution/feedback_bridge.py`（CompilerFeedbackBridge）：评分 → 反馈类型映射（>=80 thumbs_up / 60-79 comment / <60 thumbs_down），trace_id=session_id，query=draft.idea，answer=SOP 标题摘要（截断 400 字符），user_id=compiler_evaluator，comment=评测建议。模块级单例 get_default_bridge()。
- T2 端点 `app/api/orchestrate.py` dashboard 路由：评测后 bridge.record() 累积，返回 evolution 段（recent_feedback + stats）。零碰脏 main.py。
- T3 前端 `src/components/EvolutionPanel.tsx`（premium glass：累计数+好评率徽章 + 3 统计格 + 最近反馈时间线，type 徽章 thumbs_up=emerald/thumbs_down=red/comment=amber/correction=blue；时间戳本地化）+ API 类型 Evolution/EvolutionFeedback/EvolutionStats + MethodologyDashboard 栅格接入。
- T4 测试：tests/evolution/test_feedback_bridge.py(7) + tests/api/test_dashboard_evolution.py(4)。
- 全量回归 `tests/constraint|orchestrator|agent|audit|evaluation|evolution|api(test_compiler_dashboard+test_dashboard_trusted_audit+test_dashboard_evaluation+test_dashboard_evolution)` → **81 passed**（原 70 + C/Phase2 11）。`npm run check`(tsc) **0 错误**。257 脏文件未动。
- 提交 1 个（feat(evolution)），特性分支 8 文件，**待用户点头合并 master**。

## 方案 C Phase 2 合并 + 分支清理（用户 13:25 授权 A）
- 诊断安全：`feat/plan-c-phase2-self-evolution`；`MASTER..FEAT=1`、`FEAT..MASTER=0`、merge-base==master；8 个 feature 文件与 257 脏树**零重叠**；无活跃 hook。
- 执行：`git checkout master`（dirty 257 原封）→ `git merge --no-ff feat/plan-c-phase2-self-evolution -m "merge: 编译器评测接进 FeedbackStore 自进化闭环 (方案 C Phase 2)"` → merge commit **`902188b`**。
- 合并后：`MASTER..FEAT(C2)=0`（全进 master）、`FEAT(C2)..MASTER=1`（仅 merge commit）、257 脏文件原封未动。
- 合并后回归 **81 passed**（master 重跑，确认 merge 健康无回归）。
- 分支清理：`git branch --merged master` 确认全合 → `git branch -d feat/plan-c-phase2-self-evolution`（was `5793dc6`）成功。本地仅剩 `master` + 用户原有的两个 feature 分支。
- **至此 A/B/D/E + C（Phase1+Phase2）六个升级全部合并 master 落地**。整个升级系列彻底关闭：master 81 passed / tsc 0 错误 / 257 脏文件全程零触碰。无远端、无 push。

## 加远端 + push 就绪核验（用户 13:31 授权"加 remote/push"）
- 走 B（占位流程）：`git remote add origin git@github.com:PLACEHOLDER/bsc-backend.git`（SSH）。origin 已配，push 等真实 URL。

## 用户睡前 unattended 推进（用户 13:36 授权"把能做的都做了"）
- 任务 #39-#43（核验/状态报告/push 脚本/记忆/收尾）。
- **T1 核验发现两个 push 前硬卡点**（重要）：
  1. `user.name = "WorkBuddy"` / `user.email = "workbuddy@local"` → 占位身份，GitHub 会拒收 commit。push 前必改真实身份。
  2. SSH agent 未启动（`Could not open a connection to your authentication agent`）→ 真要 SSH 推需 `eval $(ssh-agent) && ssh-add`。
  - ✓ master HEAD=902188b / 81 passed / tsc 0 / 257 脏文件未动 / 无 pre-push hook / autocrlf=true。
- **T2 写最终状态报告** `docs/UPGRADE-SERIES-STATUS.md`：六个升级总览、merge commits 列表、质量门、远端占位状态、push 前硬卡点、push 操作手册（方式 A 我推 / 方式 B 自行推）。醒来照着干即可。
- **T3 加 push 助手** `scripts/push-master.sh`（特性分支 `feat/push-readiness`，1 提交 `90bf09b`）：参数化推送脚本（URL + 协议 + dry-run），自动拦截占位身份/非 master 分支/脏树，push 后跑 `git ls-remote` 核验。语法 OK + 身份守卫生效（实测拦下 `WorkBuddy/workbuddy@local`）。
- 状态报告和 push 脚本都放特性分支 `feat/push-readiness`，**待用户醒来授权合并**（不擅自动 master）。257 脏文件全程未动。

## 推送到 GitHub 最终尝试（用户 19:28 醒后）
- 用户给了真实邮箱 `hebee86@qq.com`、仓库 `https://github.com/Hebbe-Coder/bsc.git`。
- **git 身份已修正**：`user.name = "Hebbe-Coder"`、`user.email = "hebee86@qq.com"`（local scope，只影响本仓库）。
- **feat/push-readiness 已合并到 master**：`--no-ff` → merge commit `fe0a2f7`。master 当前 HEAD = `fe0a2f7`。
- **HTTPS + PAT 推送全部失败**：GitHub 已对用户账户禁用密码认证用于 Git 操作。
  - Fine-grained PAT（`github_pat_...`）→ `Invalid username or token. Password authentication is not supported for Git operations.`
  - Classic PAT（`ghp_...`）→ 同上，无论用户名用 `x-access-token` / `git` / `Hebbe-Coder`。
  - GCM 在非交互式 shell 中无法弹出浏览器认证（`/dev/tty: No such device or address`）。
- **remote URL 已清理回安全格式**：`https://github.com/Hebbe-Coder/bsc.git`（无 token 残留）。
- **结论**：唯一可行路线 = 用户在自己的终端（PowerShell/Git Bash）运行 `git push -u origin master`，GCM 会弹浏览器让 GitHub 登录，登录后自动完成推送。这是最快、最可靠的方案。
